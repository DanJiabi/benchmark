name: benchmark
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.10
  - pytorch>=2.0
  - torchvision>=0.15
  - pip
  - pip:
    - ultralytics>=8.0
    - pycocotools
    - opencv-python
    - pillow
    - pyyaml
    - pandas
    - matplotlib
    - seaborn
    - tqdm
    - rich
